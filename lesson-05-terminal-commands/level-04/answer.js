// Level 04 — Current directory (.)
// TODO: Run a script using `./` and write a short note here mentioning `.` or `./`.
export default "The `.` or `./` indicates the current directory when running a script. It tells the terminal to look for the script in the current directory and execute it.";
